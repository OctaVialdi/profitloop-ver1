
import { useMagicLink } from "./auth/useMagicLink";
import { MagicLinkParams, MagicLinkResult } from "./auth/types";

// Re-export the hook for easier access and consistent naming
export { useMagicLink as useMagicLinkProcess };
export type { MagicLinkParams, MagicLinkResult };
